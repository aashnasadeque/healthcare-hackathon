# BC Care Navigator — Environment Variables

# Anthropic API Key (get from platform.anthropic.com)
ANTHROPIC_API_KEY=

# Server config
PORT=8000
DEBUG=true
RELOAD=true

# Data directory (relative to project root)
DATA_DIR=data

# CORS (use * for hackathon, lock down in production)
ALLOWED_ORIGINS=*
